#include <stdio.h>
void main()
{
    // char *a;
    // scanf("%s", a);
    // char *b;
    // scanf("%s", b );

    // char *t;

    // t = a;
    // a = b;
    // b = t;
    // printf("a=%s\n", a);()
    // printf("b=%s\n", b);
    char a[10], b[10];
    scanf("%s%s", &a, &b);

    int i;
    for (i = 0; i<1; i++)
    {
        printf("%c", a[i]);
    }
}